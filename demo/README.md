# Public-safe demo

These artifacts summarize the independently authored public example. They are not a
performance report and are not represented as human-reviewed organizer-data results.

`public_demo.json` is deliberately reduced to fields needed to explain product behavior.
Run `python tools/check_demo_redaction.py demo/public_demo.json` before publication.
